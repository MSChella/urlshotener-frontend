// src/components/Dashboard.js
import React from 'react';

const Dashboard = () => {
    // Add logic to fetch and display URL counts per day and month
    return (
        <div>
            <h2>Dashboard</h2>
            {/* Display URL counts here */}
        </div>
    );
};

export default Dashboard;
